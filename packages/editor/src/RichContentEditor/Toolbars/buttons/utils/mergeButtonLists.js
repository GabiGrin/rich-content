/**
 *
 * @param {string[]} defaultList built-in buttonname list
 * @param {Array} pluginButtonList plugin button data { name, position } array
 * @param {Function} transformer [optional] pluginButtonList transformation function
 * @returns {string[]} merged name list
 */
export const mergeButtonLists = (defaultList, pluginButtonList, transformer = list => list) => {
// TODO: continue here..
  transformer();
};
